<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hale.bishousu">

    <application>
        <activity android:name=".activity.BishousuActivity"></activity>
        <receiver
            android:name="com.hale.bishousu.receiver.TimerReceiver"
            android:process=":remote"
           >
            <intent-filter >
                <action android:name="ABC"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>